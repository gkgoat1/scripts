#!/bin/sh
copilot -p "Descriptively commit the staged changes. If multiple repos are present in subdirectories, commit to all of them. Do NOT use shell scripts; `git` is the only allowed command" --allow-tool 'shell(git)'

